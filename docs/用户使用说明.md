# Rexion 用户使用说明

> 面向终端的 DeepSeek 原生 AI coding agent —— 配置与插件驱动的极薄 harness，单一静态 Go 二进制，围绕 DeepSeek 前缀缓存调优。

本文档按模块功能组织，覆盖命令行、桌面端、内置工具、MCP 插件、技能系统、权限沙箱等全部功能。

---

## 目录

- [一、安装与快速开始](#一安装与快速开始)
- [二、配置系统](#二配置系统)
- [三、命令行（CLI）](#三命令行cli)
- [四、会话内斜杠命令](#四会话内斜杠命令)
- [五、内置工具](#五内置工具)
- [六、MCP 插件系统](#六mcp-插件系统)
  - [6.1 Office 文档插件](#61-office-文档插件)
  - [6.2 Sheet 表格插件](#62-sheet-表格插件)
  - [6.3 Search 搜索插件](#63-search-搜索插件)
  - [6.4 Mail 邮件插件](#64-mail-邮件插件)
  - [6.5 IM 即时通讯插件](#65-im-即时通讯插件)
  - [6.6 Calendar 日历待办插件](#66-calendar-日历待办插件)
  - [6.7 Slides 幻灯片插件](#67-slides-幻灯片插件)
- [七、技能（Skills）系统](#七技能skills系统)
- [八、自定义命令](#八自定义命令)
- [九、@ 引用机制](#九-引用机制)
- [十、桌面端使用](#十桌面端使用)
- [十一、权限与沙箱](#十一权限与沙箱)
- [十二、记忆系统](#十二记忆系统)
- [十三、双模型协同](#十三双模型协同)
- [十四、故障排查](#十四故障排查)

---

## 一、安装与快速开始

### 1.1 安装

```sh
# 任意系统（自动拉取对应平台原生二进制）
npm i -g Rexion

# macOS
brew install esengine/Rexion/Rexion
```

预编译归档（`darwin|linux|windows × amd64|arm64`）和 `SHA256SUMS` 见每个 [GitHub release](https://github.com/esengine/DeepSeek-Rexion/releases)。

### 1.2 从源码构建

```sh
make build      # -> bin/Rexion(.exe)
make cross      # -> dist/（六平台交叉编译）
```

特性：`CGO_ENABLED=0` 单二进制，唯一依赖是一个 TOML 解析库。

### 1.3 快速开始

```sh
Rexion setup                      # 配置向导 → ./Rexion.toml
export DEEPSEEK_API_KEY=sk-...      # 或写入 .env（见 .env.example）
Rexion chat                       # 进入交互式会话，运行 /init 生成 AGENTS.md
Rexion run "把 main.go 里的 TODO 实现掉"
Rexion run --model mimo-pro "给这个函数补单元测试"
echo "解释这段代码" | Rexion run    # 管道输入
```

---

## 二、配置系统

### 2.1 配置优先级

**flag > `./Rexion.toml` > `~/.config/Rexion/config.toml` > 内置默认值**

密钥经环境变量通过 `api_key_env` 注入，绝不写入配置文件。

### 2.2 Rexion.toml 完整示例

```toml
default_model = "deepseek-flash"   # 执行器；设 [agent].planner_model 可加规划器
# language    = "zh"               # 界面语言；为空则按 $LANG / $REXION_LANG 自动检测

[agent]
# planner_model = "mimo-pro"          # 可选的低频规划器
# subagent_model = "deepseek-pro"     # runAs=subagent skill 的默认模型
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }
auto_plan = "off"                  # off|on；off 表示计划模式仅手动开启
# auto_plan_classifier = "deepseek-flash"   # 可选；只在边界任务上调用

[[providers]]
name        = "deepseek-flash"
kind        = "openai"
base_url    = "https://api.deepseek.com"
model       = "deepseek-v4-flash"
api_key_env = "DEEPSEEK_API_KEY"
# 预设：deepseek-pro、mimo-pro、mimo-flash @ api.xiaomimimo.com/v1

# 自定义供应商（任何 OpenAI-compatible 端点）
[[providers]]
name        = "my-proxy"
kind        = "openai"
base_url    = "https://my-proxy.example.com/v1"
models      = ["gpt-4o", "claude-3-opus"]
default     = "gpt-4o"
api_key_env = "MY_PROXY_API_KEY"

# Anthropic 原生供应商
[[providers]]
name        = "claude"
kind        = "anthropic"
model       = "claude-sonnet-4-20250514"
api_key_env = "ANTHROPIC_API_KEY"

[tools]
enabled = []                   # 省略/为空 = 全部内置工具
bash_timeout_seconds = 120     # 前台安全上限；0 表示不设工具层超时

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # 额外自定义技能目录
# excluded_paths = ["~/.agents/skills"]         # 隐藏约定来源
# disabled_skills = ["review"]                  # 隐藏技能，直到 /skill enable <name>

[permissions]
mode  = "ask"                                # 兜底：ask|allow|deny
deny  = ["bash(rm -rf*)", "bash(git push*)"] # 任何模式下都硬阻断
allow = ["bash(go test*)"]                   # 从不询问

[sandbox]
# workspace_root = ""          # 文件写工具被限制在此目录；留空 = 当前目录
# allow_write    = ["/tmp"]    # write_file/edit_file/multi_edit 额外可写目录

[[plugins]]
name    = "office"
command = "Rexion-plugin-office"
```

### 2.3 内置 Provider 预设

| Provider | kind | base_url | 说明 |
|----------|------|----------|------|
| `deepseek-flash` | openai | api.deepseek.com | DeepSeek 执行器 |
| `deepseek-pro` | openai | api.deepseek.com | DeepSeek 规划器 |
| `mimo-pro` | openai | api.xiaomimimo.com/v1 | MiMo v2.5-pro |
| `mimo-flash` | openai | api.xiaomimimo.com/v1 | MiMo v2-flash |
| OpenAI | openai | api.openai.com | 官方预设 |
| Anthropic | anthropic | api.anthropic.com | 官方预设 |
| Gemini | openai | generativelanguage.googleapis.com | 官方预设 |
| Qwen | openai | dashscope.aliyuncs.com | 官方预设 |

`base_url` 不含版本段（如 `/v1`）时运行时自动补全。

---

## 三、命令行（CLI）

入口位于 [internal/cli/cli.go](../internal/cli/cli.go)，`Run()` 通过 `switch cmd` 分发顶层子命令。

### 3.1 顶层子命令

| 子命令 | 用途 | 主要参数 |
|--------|------|----------|
| `run` | 一次性 headless 执行：接收 prompt → 运行 agent → 输出结果 | `--model`, `--max-steps`, `--show-thinking`, `--metrics`, `--dir`, `--sandbox`, `--format (text\|json)`, `--timeout`, `--approval-mode (auto\|ask)`, `--diff`, `--quiet` |
| `chat` / `code` | 交互式会话（Bubbletea v2 TUI）；`code` 是 v0.x 遗留别名 | `--model`, `--max-steps`, `--continue/-c`, `--resume`, `--dangerously-skip-permissions/--yolo`, `--dir`, `--sandbox` |
| `serve` | HTTP+SSE 服务模式，事件流推送到浏览器 | `--model`, `--max-steps`, `--addr`（默认 `127.0.0.1:8787`）, `--resume` |
| `setup` | 配置向导：选语言 → 选 provider → 录入 API key | `--local/-l`（写入 `./Rexion.toml`） |
| `config` | 配置子命令（目前仅 `auto-plan`） | `config auto-plan [--local] [off\|on]` |
| `init` | 项目记忆入口（实际由会话内 `/init` skill 生成 `AGENTS.md`） | — |
| `acp` | ACP（Agent Communication Protocol）命令 | — |
| `mcp` | MCP 服务器配置管理（见下） | — |
| `mcp-server` | 将 Rexion 自身作为 MCP 工具服务器暴露 | `--transport (stdio\|http)`, `--addr`（默认 `0.0.0.0:9090`）, `--model`, `--max-steps`, `--dir`, `--sandbox` |
| `codegraph` | 代码图谱生成 | — |
| `doctor` | 健康检查，输出诊断报告 | `--json`（JSON 格式） |
| `version` / `--version` / `-v` | 输出版本号 | — |
| `help` / `--help` / `-h` | 输出 usage | — |

### 3.2 mcp 子命令

```sh
Rexion mcp list                          # 列出已配置的 MCP 服务器
Rexion mcp add <name> [command [args...]] # 添加 stdio 服务器
Rexion mcp add <name> --http URL          # 添加 HTTP 服务器
Rexion mcp add <name> --sse URL           # 添加 SSE 服务器
  # 通用 flag：--env K=V ... --header K=V ...
Rexion mcp remove <name>                  # 移除
Rexion mcp import                         # 从 cc-switch 导入
```

### 3.3 使用示例

```sh
# 一次性执行
Rexion run "修复 login.go 里的空指针"
Rexion run --model mimo-pro --metrics "重构这个函数"

# 服务模式
Rexion serve --addr 0.0.0.0:8787

# 把自己暴露成 MCP 服务器
Rexion mcp-server --transport http --addr 0.0.0.0:9090

# 诊断
Rexion doctor --json
```

---

## 四、会话内斜杠命令

在 `Rexion chat` 中使用。内置命令在本地执行，自定义命令见[第八节](#八自定义命令)。

| 命令 | 用途 |
|------|------|
| `/new` | 新建会话 |
| `/compact` | 手动压缩上下文（低频自动压缩也会触发） |
| `/rewind` | 两阶段 picker：选 turn → 选恢复范围（也可按 Esc-Esc） |
| `/tree` | 查看已保存的对话分支树 |
| `/branch [name]` | 从当前对话末端分支 |
| `/branch <turn> [name]` | 从较早的 checkpoint 轮次分支 |
| `/switch <id\|name>` | 切换到另一个分支 |
| `/resume` | 无参列出最近 10 个会话；`/resume <n>` 加载第 n 个 |
| `/todo` | 查看 / 管理 todo 列表 |
| `/model` | 无参列出所有 provider/model；`/model <ref>` 切换会话模型（保留上下文） |
| `/effort` | 切换推理 effort |
| `/memory` | 显示当前加载的记忆 |
| `/forget <name>` | 删除某条自动记忆 |
| `/skills list` | 列出所有 skill |
| `/skills show <name>` | 查看 skill 详情 |
| `/skills enable <name>` / `/skills disable <name>` | 启用/禁用 skill |
| `/skills new` / `/skills init <name> [--global]` | 创建新 skill |
| `/skills paths` | 显示 skill 发现目录 |
| `/hooks list` | 列出 hook |
| `/hooks trust <name>` | 信任 hook |
| `/mcp` | 列出已连接 MCP 服务器及暴露的工具/prompt/resource |
| `/auto-plan off\|on` | 修改用户级计划模式设置 |
| `/help` | 帮助 |

---

## 五、内置工具

内置工具位于 [internal/tool/builtin/](../internal/tool/builtin/)，通过 `init()` 自注册。全部注册工具共 23 个，下面按功能族分组。

### 5.1 核心文件/Shell 工具（9 个）

| 工具 | 只读 | 关键参数 | 说明 |
|------|------|----------|------|
| `read_file` | ✅ | `path`*, `offset`(0-based), `limit`(默认 2000) | 支持 BOM/UTF-16 自动检测、二进制探测 |
| `write_file` | ❌ | `path`*, `content`* | 保留原文件编码（GBK/UTF-16/BOM），自动建父目录 |
| `edit_file` | ❌ | `path`*, `old_string`*(唯一), `new_string`* | 精确字符串替换 |
| `multi_edit` | ❌ | `path`*, `edits`*(`[{old_string,new_string,replace_all}]`) | 原子性批量编辑，全成才写盘 |
| `bash` | ❌ | `command`*, `run_in_background` | Windows 无 bash 自动用 PowerShell；沙箱可选 |
| `ls` | ✅ | `path`(默认 "."), `recursive` | 列目录 |
| `glob` | ✅ | `pattern`* | 支持 `* ? []` 和 `**`；上限 1000 |
| `grep` | ✅ | `pattern`*(RE2), `path`(默认 ".") | 优先用 ripgrep；上限 200 |
| `web_fetch` | ✅ | `url`* | SSRF 防护（拒绝内网地址），HTML 提文本，1MiB 上限 |

`*` 表示必填。

### 5.2 后台任务族（3 个）

| 工具 | 说明 |
|------|------|
| `bash_output` | 读取后台 bash job 的新输出 |
| `kill_shell` | 终止后台 job |
| `wait` | 等待后台 job 完成 |

### 5.3 代码编辑族（4 个）

| 工具 | 说明 |
|------|------|
| `apply_patch` | 应用 unified/patch 格式补丁 |
| `delete_range` | 删除指定行范围 |
| `delete_symbol` | 删除代码符号 |
| `notebook_edit` | 编辑 Jupyter notebook |

### 5.4 文档生成族（3 个）

| 工具 | 说明 |
|------|------|
| `write_docx` | 内置写 docx（与 office 插件互补） |
| `write_pdf` | 生成 PDF |
| `write_sheet` | 内置写表格（与 sheet 插件互补） |

### 5.5 Eval 族（2 个）

| 工具 | 说明 |
|------|------|
| `js_eval` | 执行 JavaScript |
| `python_eval` | 执行 Python |

### 5.6 规划族（2 个）

| 工具 | 说明 |
|------|------|
| `complete_step` | 标记规划步骤完成 |
| `todo_write` | 写入 / 更新 todo 列表 |

---

## 六、MCP 插件系统

Rexion 是一个 MCP 客户端。`[[plugins]]` 的 `type` 选择传输：`stdio`（默认）启动本地子进程；`http`（Streamable HTTP）连接远程 `url`。工具以 `mcp__<server>__<tool>` 暴露给模型，与 Claude Code 一致；声明 `readOnlyHint: true` 的工具参与并行调度并命中权限层只读默认放行。

### 6.0.1 插件配置

```toml
[[plugins]]                       # 本地 stdio 服务器
name    = "office"
command = "Rexion-plugin-office"
# args    = ["--flag"]
# env     = { KEY = "value" }
# tools   = ["read_docx"]          # 可选：限制可见工具

[[plugins]]                       # 远程 Streamable HTTP
name    = "stripe"
type    = "http"
url     = "https://mcp.stripe.com"
headers = { Authorization = "Bearer ${STRIPE_KEY}" }   # ${VAR} / ${VAR:-default} 展开
```

### 6.0.2 兼容 Claude `.mcp.json`

把 `.mcp.json` 放到项目根目录，Rexion 会原样读取——其 `mcpServers` 规范与 `[[plugins]]` 字段一一对应。两处来源合并加载；同名时以 `Rexion.toml` 为准。

```json
{
  "mcpServers": {
    "filesystem": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path"] }
  }
}
```

### 6.0.3 旧版兼容

`0.x` 的 `~/.Rexion/config.json` 仍被读取（读其 `mcpServers`、遵从 `mcpDisabled`），作为最低优先级来源。

### 6.0.4 MCP 服务器三要素

| 要素 | 暴露形式 |
|------|----------|
| **tools** | `mcp__<server>__<tool>` 工具调用 |
| **prompts** | `/mcp__<server>__<prompt>` 斜杠命令（空格分隔参数） |
| **resources** | `@<server>:<uri>` 在消息中引用拉入上下文 |

---

### 6.1 Office 文档插件

**插件**：`Rexion-plugin-office`（[cmd/Rexion-plugin-office](../cmd/Rexion-plugin-office/)）

文档处理能力：读取 `.docx`、写入 `.docx`、渲染文本模板、Markdown 转 PDF。零第三方依赖（docx 读写仅用标准库 `archive/zip` + `encoding/xml`）。

#### 工具清单

| 工具 | 模式 | 依赖 | 说明 |
|------|------|------|------|
| `mcp__office__read_docx` | 只读 | 无 | 读 .docx 返回 Markdown |
| `mcp__office__write_docx` | 读写 | 无 | Markdown 写入 .docx |
| `mcp__office__render_template` | 只读 | 无 | 渲染 Go text/template |
| `mcp__office__md_to_pdf` | 读写 | pandoc（可选） | Markdown 转 PDF |

#### read_docx 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | .docx 绝对路径 |
| `mode` | string | ❌ | `full`（默认，全文）或 `outline`（仅标题） |
| `max_chars` | integer | ❌ | 输出字符上限（默认 20000） |

```
mcp__office__read_docx(path="C:/docs/报告.docx", mode="outline")
```

#### write_docx 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | 输出 .docx 路径 |
| `content` | string | ✅ | Markdown 内容（`#`/`##`/`###` → Heading 样式，管道表 → 原生表格） |
| `title` | string | ❌ | 文档标题元数据 |

#### render_template 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `template` | string | 二选一 | 内联模板源 |
| `template_path` | string | 二选一 | 模板文件路径 |
| `variables` | object | ❌ | 变量映射 `{"key":"value"}` |

模板语法为 Go [text/template](https://pkg.go.dev/text/template)，`missingkey=error` 强制校验未定义变量：

```
Hello {{.name}}, you have {{len .tasks}} tasks:
{{- range .tasks}}
- {{.}}
{{- end}}
Status: {{if .done}}complete{{else}}pending{{end}}
```

#### md_to_pdf 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `output` | string | ✅ | 输出 .pdf 路径 |
| `input` | string | 二选一 | 输入文件（.md/.html/.docx） |
| `markdown` | string | 二选一 | 内联 Markdown |
| `pdf_engine` | string | ❌ | `pdflatex`/`wkhtmltopdf`/`weasyprint`/`tectonic` |
| `extra_args` | array | ❌ | 额外 pandoc 参数 |

> 仅当 PATH 中能找到 `pandoc` 时才注册该工具。

#### 配置

```toml
[[plugins]]
name    = "office"
command = "Rexion-plugin-office"
# tools = ["read_docx", "render_template"]   # 只读 profile
```

---

### 6.2 Sheet 表格插件

**插件**：`Rexion-plugin-sheet`（[cmd/Rexion-plugin-sheet](../cmd/Rexion-plugin-sheet/)）

表格能力：读取、写入、SQL 风格查询、图表。依赖 `xuri/excelize`（xlsx）和 `wcharczuk/go-chart`（图表）。

#### 工具清单

| 工具 | 模式 | 说明 |
|------|------|------|
| `mcp__sheet__read_sheet` | 只读 | 读 xlsx/csv 行数据，返回 Markdown/JSON/CSV |
| `mcp__sheet__write_sheet` | 读写 | 写 xlsx/csv，支持 overwrite/append |
| `mcp__sheet__query_sheet` | 只读 | SQL 风格聚合查询（WHERE/GROUP BY/ORDER BY/LIMIT） |
| `mcp__sheet__chart_sheet` | 只读 | 生成 bar/line/pie/scatter 图表 PNG（base64） |

#### read_sheet 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | xlsx/csv 路径 |
| `sheet` | string | ❌ | 工作表名（xlsx） |
| `range` | string | ❌ | Excel 范围（如 `A1:Z100`） |
| `header_row` | integer | ❌ | 标题行号（默认 1） |
| `max_rows` | integer | ❌ | 行数上限（默认 1000，防 token 溢出） |
| `format` | string | ❌ | `markdown`/`json`/`csv`（默认 markdown） |

#### write_sheet 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | 输出路径 |
| `data` | array/string | ✅ | 二维数组或 Markdown 表格 |
| `sheet` | string | ❌ | 工作表名（默认 Sheet1） |
| `mode` | string | ❌ | `overwrite`（默认）或 `append` |
| `start_cell` | string | ❌ | 起始单元格（默认 A1） |

#### query_sheet 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | xlsx/csv 路径 |
| `select` | array | ✅ | 字段或聚合（如 `["region","SUM(amount) as total"]`） |
| `where` | string | ❌ | 过滤条件 |
| `group_by` | array | ❌ | 分组字段 |
| `order_by` | string | ❌ | 排序（如 `total DESC`） |
| `limit` | integer | ❌ | 结果行数上限（默认 100） |

**WHERE 语法**：`= != > >= < <= IN (...)`，逻辑 `AND OR` 与括号。

```
region='华东' AND amount>1000
region IN ('华东','华北')
(region='华北' OR amount>5000) AND status='active'
```

**聚合函数**：`SUM` `COUNT` `AVG` `MIN` `MAX`

#### chart_sheet 参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `path` | string | ✅ | xlsx/csv 路径 |
| `x` | string | ✅ | X 轴列名 |
| `y` | array | ✅ | Y 轴列名数组 |
| `type` | string | ❌ | `bar`/`line`/`pie`/`scatter`（默认 bar） |
| `title` | string | ❌ | 图表标题 |
| `sheet` | string | ❌ | 工作表名 |
| `range` | string | ❌ | 数据范围 |

```
mcp__sheet__chart_sheet(path="销售.xlsx", x="月份", y=["销售额","利润"], type="line", title="销售趋势")
```

#### 配置

```toml
[[plugins]]
name    = "sheet"
command = "Rexion-plugin-sheet"
```

---

### 6.3 Search 搜索插件

**插件**：`Rexion-plugin-search`（[cmd/Rexion-plugin-search](../cmd/Rexion-plugin-search/)）

| 工具 | 模式 | 依赖 | 说明 |
|------|------|------|------|
| `mcp__search__web_search` | 只读 | `SEARCH_API_KEY` | 网页搜索 |
| `mcp__search__web_extract` | 只读 | 无 | 抽取网页正文 |
| `mcp__search__compare_table` | 只读 | 无 | 生成对比表 |

#### web_search

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | ✅ | 查询词 |
| `max_results` | integer | ❌ | 默认 5，上限 20 |

环境变量：`SEARCH_API_KEY`（必填），`SEARCH_API_PROVIDER=serpapi|bing`（可选）。

#### web_extract

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `url` | string | ✅ | 目标 URL |
| `extract_goal` | string | ❌ | 抽取目标提示（如 `'pricing'`、`'features'`） |

#### compare_table

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `dimensions` | array | ✅ | 对比维度 |
| `items` | array | ✅ | `[{name, data}]` |
| `output_format` | string | ❌ | `markdown`/`xlsx`（默认 markdown） |

---

### 6.4 Mail 邮件插件

**插件**：`Rexion-plugin-mail`（[cmd/Rexion-plugin-mail](../cmd/Rexion-plugin-mail/)）

**环境变量**：`MAIL_IMAP_HOST/USER/PASS`、`MAIL_SMTP_HOST/USER/PASS`

| 工具 | 模式 | 说明 |
|------|------|------|
| `mcp__mail__read_mail` | 只读 | 读取邮件 |
| `mcp__mail__send_mail` | 读写 | 发送邮件 |
| `mcp__mail__search_mail` | 只读 | 搜索邮件 |
| `mcp__mail__classify_mail` | 只读 | 分类（urgent/action/newsletter/personal/other） |
| `mcp__mail__oauth2_authorize` | 只读 | OAuth2 授权（gmail/outlook） |
| `mcp__mail__oauth2_callback` | 读写 | OAuth2 回调换 token |

#### read_mail

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folder` | string | ❌ | 默认 INBOX |
| `limit` | integer | ❌ | 默认 10 |
| `offset` | integer | ❌ | 默认 0 |

#### send_mail

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `to` | string | ✅ | 收件人 |
| `subject` | string | ✅ | 主题 |
| `body` | string | ✅ | 正文 |
| `cc` | string | ❌ | 抄送 |
| `reply_to_message_id` | string | ❌ | 回复的邮件 ID |

#### search_mail

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `query` | string | ✅ | 搜索词 |
| `folder` | string | ❌ | 默认 INBOX |
| `limit` | integer | ❌ | 默认 10 |

#### classify_mail

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `folder` | string | ❌ | 默认 INBOX |
| `limit` | integer | ❌ | 默认 20 |

---

### 6.5 IM 即时通讯插件

**插件**：`Rexion-plugin-im`（[cmd/Rexion-plugin-im](../cmd/Rexion-plugin-im/)）

支持企业微信（WeCom）、飞书（Feishu）、钉钉（DingTalk）三平台。

**环境变量**：`IM_BOT_PORT`（默认 9876）、`IM_WECOM_KEY`、`IM_FEISHU_KEY`、`IM_DINGTALK_KEY`、`IM_DINGTALK_SECRET`

| 工具 | 说明 |
|------|------|
| `mcp__im__start_bot` | 启动 IM bot |
| `mcp__im__stop_bot` | 停止 bot |
| `mcp__im__send_message` | 发送消息 |
| `mcp__im__list_pending_commands` | 列出待处理命令（只读） |
| `mcp__im__mark_command_done` | 标记命令完成 |

#### start_bot

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `port` | integer | ❌ | 默认 9876 |
| `platforms` | array | ❌ | 默认 `["wecom"]`，可选 `wecom|feishu|dingtalk` |
| `token` | string | ❌ | 鉴权 token |

#### send_message

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `platform` | string | ✅ | `wecom|feishu|dingtalk` |
| `webhook_url` | string | ✅ | webhook 地址 |
| `content` | string | ✅ | 消息内容 |
| `msg_type` | string | ❌ | `text|markdown`（默认 text） |

---

### 6.6 Calendar 日历待办插件

**插件**：`Rexion-plugin-calendar`（[cmd/Rexion-plugin-calendar](../cmd/Rexion-plugin-calendar/)）

- **日历集成**：Windows 用 Outlook COM（PowerShell），macOS 用 icalBuddy/AppleScript
- **Todo 存储**：SQLite `$REXION_DATA_DIR/data.db`（默认 `~/.Rexion/data.db`）

| 工具 | 模式 | 说明 |
|------|------|------|
| `mcp__calendar__read_event` | 只读 | 读取日历事件 |
| `mcp__calendar__add_event` | 读写 | 添加事件 |
| `mcp__calendar__list_todo` | 只读 | 列出 todo |
| `mcp__calendar__add_todo` | 读写 | 添加 todo |
| `mcp__calendar__update_todo` | 读写 | 更新 todo |

#### read_event

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `start_date` | string | ✅ | ISO 8601 开始日期 |
| `end_date` | string | ✅ | ISO 8601 结束日期 |

#### add_event

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 标题 |
| `start_time` | string | ✅ | ISO 8601 datetime |
| `end_time` | string | ✅ | ISO 8601 datetime |
| `description` | string | ❌ | 描述 |
| `location` | string | ❌ | 地点 |

#### list_todo / add_todo / update_todo

| 参数 | 说明 |
|------|------|
| `list_todo`: `status`(`pending|done|""`), `limit`(默认 20) |
| `add_todo`: `title`*, `description`, `due_date`(ISO 8601), `priority`(`low|medium|high`，默认 medium) |
| `update_todo`: `id`*, `status`(`pending|done`), `title`, `due_date` |

---

### 6.7 Slides 幻灯片插件

**插件**：`Rexion-plugin-slides`（[cmd/Rexion-plugin-slides](../cmd/Rexion-plugin-slides/)）

依赖 `github.com/unidoc/unioffice/v2`，需要 `UNIDOC_LICENSE_API_KEY`（缺失则带水印运行）。

| 工具 | 说明 |
|------|------|
| `mcp__slides__create_ppt` | 从 Markdown 大纲创建 PPT |
| `mcp__slides__add_slide` | 添加 slide |
| `mcp__slides__apply_theme` | 应用主题 |
| `mcp__slides__add_chart` | 添加图表 slide |
| `mcp__slides__export_pdf` | 导出 PDF |

#### create_ppt

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `outline` | string | ✅ | Markdown 大纲（`##` 为 slide 标题） |
| `output_path` | string | ✅ | 输出 `.pptx` 路径 |
| `title` | string | ❌ | 演示文稿标题 |
| `style` | string | ❌ | `professional|creative|minimal`（默认 professional） |

#### add_slide

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `ppt_path` | string | ✅ | pptx 文件路径 |
| `title` | string | ✅ | slide 标题 |
| `content` | string | ❌ | 正文 |
| `notes` | string | ❌ | 演讲者备注 |
| `layout` | string | ❌ | `title|title_content|blank`（默认 title_content） |

#### apply_theme / add_chart / export_pdf

| 参数 | 说明 |
|------|------|
| `apply_theme`: `ppt_path`*, `theme`*(`professional|creative|minimal`) |
| `add_chart`: `ppt_path`*, `chart_type`*(`bar|line|pie`), `title`*, `data`*(JSON: `{"labels":[...], "values":[...]}` 或多系列 `[[...],[...]]`), `position`(integer) |
| `export_pdf`: `ppt_path`*, `output_path`(默认同名 `.pdf`) |

---

## 七、技能（Skills）系统

Skills 是可复用的 prompt 工作流定义，位于 [internal/skill/skill.go](../internal/skill/skill.go)。

### 7.1 Skill 文件格式

Markdown 文件 + YAML frontmatter，放在 `.Rexion/skills/` 下。文件名 `<name>.md` 或目录 `<name>/SKILL.md`。

```markdown
---
name: weekly-report
description: 基于最近 7 天的 git log 自动生成周报 docx 文件
runas: subagent
allowed-tools: bash, read_file, mcp__office__write_docx
model: deepseek-pro
effort: high
---
（prompt body 正文）
```

### 7.2 frontmatter 字段

| 字段 | 说明 |
|------|------|
| `name` | 规范标识符（缺失则用文件名） |
| `description` | 一行描述（缺失则不进入模型索引） |
| `runas` | `subagent` 触发隔离子 agent 运行；默认 inline |
| `allowed-tools` | 逗号分隔工具白名单（仅对 subagent 生效） |
| `model` | subagent 模式下的模型覆盖 |
| `effort` | subagent 模式下的 effort |
| `context` | `fork` 等价于 `runas: subagent` |
| `agent` | 非空值等价于 `runas: subagent` |

### 7.3 发现目录

按优先级（高覆盖低）：`.Rexion/skills/`（项目）> 自定义路径 > `~/.config/Rexion/skills/`（用户全局）> 内置。

兼容扫描：`.agents/`、`.agent/`、`.claude/`（支持 Claude 等工具的 skill 迁移）。

### 7.4 调用方式

- 直接调用：`/<skill-name>`（如 `/weekly-report`）
- 管理命令：`/skills list|show|enable|disable|new|paths`

**两种运行模式**：
- **inline**（默认）：skill body 折叠为 tool result 注入当前会话
- **subagent**：在隔离子循环中运行，仅返回最终答案，保护主会话上下文

### 7.5 内置 Skill 示例

| Skill | 触发方式 | 工作流 | 输出 |
|-------|----------|--------|------|
| `weekly-report` | `/skill weekly-report` 或「帮我写周报」 | git log → 读 AGENTS.md → 归类 → write_docx | `周报_YYYYWW.docx` |
| `meeting-minutes` | `/skill meeting-minutes` | 解析会议转写 → 结构化输出 | 会议纪要 Markdown/docx |
| `contract-draft` | `/skill contract-draft` | 选类型 → 填关键信息 → render_template → write_docx | `合同_<类型>_<日期>.docx` + TODO.md |
| `sheet-clean` | `/skill sheet-clean` | 去重/标准化/缺失值填充 | `<原名>_cleaned.xlsx` |
| `sheet-analysis` | `/skill sheet-analysis` | 统计/分组聚合/相关性/图表 | 统计摘要 + 图表 PNG |

---

## 八、自定义命令

放在 `.Rexion/commands/`（项目）或 `~/.config/Rexion/commands/`（用户）下的 Markdown 文件，frontmatter 支持 `description` 与 `argument-hint`。子目录构成命名空间（`git/commit.md` → `/git:commit`）。

```markdown
---
description: Review the staged diff
argument-hint: [focus-area]
---
Review the staged diff. Focus on $ARGUMENTS, list bugs with file:line.
```

- `$ARGUMENTS` 展开为全部空格分隔参数
- `$1`…`$N` 为位置参数
- MCP prompts 也以 `/mcp__<server>__<prompt>` 形式出现

---

## 九、@ 引用机制

在消息里写 `@` 引用，发送前解析成带标签的上下文块：

| 写法 | 含义 |
|------|------|
| `@path/to/file` | 注入本地文件内容 |
| `@dir` | 注入目录清单 |
| `@<server>:<uri>` | 注入 MCP 资源 |

本地路径**只有真实存在**时才当作引用，普通 `@mention` 保持原文。敲 `/` 或 `@` 会弹出补全菜单：斜杠命令，或**逐层**文件导航（一次只列当前一层目录、可下钻）外加 MCP 资源。

---

## 十、桌面端使用

桌面端基于 Wails（Go 内核 + React/TS webview），同一个 `control.Controller` 直接绑定到 webview，无 HTTP 跳跃。开发见 [desktop/README.md](../desktop/README.md)。

### 10.1 开发与构建

```sh
cd desktop
wails dev            # 热重载（Go + Vite）
# 仅前端
cd desktop/frontend && pnpm install && pnpm dev   # 浏览器中开发（mock 桥）

wails build          # → build/bin/Rexion(.app/.exe)
# Linux WebKitGTK 4.1（Fedora 40+/Ubuntu 24.04+/Arch）
wails build -tags webkit2_41
```

### 10.2 工作区类型

桌面端按 `workspaceType` 切换三套布局：`coding`（开发）、`office`（办公）、`assistant`（助手）。

### 10.3 侧边栏导航

侧边栏按 4 个 section 组织（[Sidebar.tsx](../desktop/frontend/src/components/Sidebar.tsx)）：

| Section | 导航项 | 触发行为 | 对应面板 |
|---------|--------|----------|----------|
| 顶部 | 新建会话 | `onNewSession()` | — |
| Home | Home | `onNavigate("home")` | `HomePanel` |
| Workbench (coding) | ProjectTree | 内嵌渲染 | ProjectTree |
| Workbench (office) | OfficePanel | 内嵌渲染 | OfficePanel |
| Workbench (assistant) | Calendar / Todos | `onNavigate("calendar")` / `onNavigate("todos")` | `CalendarPanel` |
| Resources | 文件管理 / 模板库 / 插件 | `onNavigate("files")` / `onOpenTemplates` / `onOpenSettingsTab("mcp")` | 右侧 dock / 弹窗 / Settings |
| Management | 全部历史 | `onOpenAllHistory()` | History 视图 |
| Management | 定时任务 | `onNavigate("scheduled")` | `SchedulerPanel` |
| Management | Trace | `onNavigate("trace")` | `AgentCanvas` |
| Management | 记忆 | `onNavigate("memory")` | Settings → memory tab |
| Management | Repo Wiki | `onOpenRepoWiki` | RepoWiki 弹窗 |
| Management | 回收站 | `onOpenTrash()` | Trash 视图 |
| Management | 设置 | `onOpenSettings` | SettingsPanel |

### 10.4 主区域面板（navPage）

| navPage | 渲染组件 |
|---------|----------|
| `home` | `<HomePanel>` |
| `calendar` / `todos` | `<CalendarPanel>`（按 tabId 切换） |
| `scheduled` | `<SchedulerPanel>` |
| `trace` | `<AgentCanvas>`（agent 执行流程可视化，基于 `parentId` 工具调用嵌套的运行时投影） |
| 默认 | `<Transcript>`（会话流，含可选 `<ProgressStepper>`） |

> `files` 与 `memory` 不进入主区域，分别打开右侧工作区 dock 和设置中心。

### 10.5 设置中心

设置面板（[SettingsPanel.tsx](../desktop/frontend/src/components/SettingsPanel.tsx)）共 10 个标签页：

| Tab | 配置项 |
|-----|--------|
| **general** | 语言、关闭行为（background\|quit）、autoPlan（off\|on） |
| **models** | usage 子页：defaultModel / plannerModel / subagentModel / subagentEffort；access 子页：添加/配置/移除 provider，官方预设一键配置，测试并抓取模型列表 |
| **mcp** | MCP 服务器配置 |
| **skills** | Skills 管理 |
| **memory** | 记忆配置 |
| **permissions** | mode(ask\|allow\|deny)、deny/ask/allow 规则 |
| **sandbox** | bash(enforce\|off)、network、workspaceRoot、allowWrite |
| **network** | proxyMode(auto\|custom\|off)、proxyType/server/port/username/password/proxyUrl/noProxy |
| **appearance** | theme(auto\|light\|dark)、themeStyle、textSize、fontFamily、unifiedTheme |
| **updates** | 版本检查与自动更新 |

### 10.6 模板库

- 位置：侧边栏「模板库」图标（office 工作区）
- 浏览 `.Rexion/templates/` 下的模板文件
- 按类型筛选：Word / 表格 / Markdown / 模板 / 文本 / CSV
- 「套用」插入 composer；「打开」用默认应用打开
- 支持格式：`.docx`、`.xlsx`、`.md`、`.tmpl`、`.txt`、`.csv`

### 10.7 文档预览

Agent 生成 docx/图片后，ToolCard 自动显示预览：
- 图片：内联渲染
- docx/pdf：下载 chip + 「用默认应用打开」
- 「导出到工作区」保存到指定路径

### 10.8 审批机制

有副作用的工具调用（文件写、命令执行）会触发 [ApprovalModal.tsx](../desktop/frontend/src/components/ApprovalModal.tsx) 审批弹窗，用户可选择：本次允许 / 本会话允许 / 拒绝。

### 10.9 自动更新

- 启动时检查 `latest.json`（R2 优先，GitHub 兜底），有新版显示更新横幅
- Settings → Software update 可手动检查
- **Linux/Windows**：下载并验证 minisign 签名后原地更新（Windows 走 per-user NSIS 安装器，免管理员权限）
- **macOS**：暂不支持自更新（未签名），横幅链接到下载页

---

## 十一、权限与沙箱

### 11.1 权限（策略层）

逐次调用把关，优先级：`deny` > `ask` > `allow` > 兜底 `mode`。

- 只读工具永远 allow
- writer 工具落到 `mode`（ask/allow/deny）
- `Rexion chat` 在 writer 调用前询问（`1` 本次 · `2` 本会话 · `3` 拒绝，兼容 `y/a/n`）
- `Rexion run` 自主运行但遵守 `deny`

```toml
[permissions]
mode  = "ask"
deny  = ["bash(rm -rf*)", "bash(git push*)"]
allow = ["bash(go test*)"]
```

### 11.2 沙箱（强制层）

文件写工具（`write_file` / `edit_file` / `multi_edit`）拒绝 `[sandbox] workspace_root` 之外的任何路径（默认当前目录），并解析符号链接与 `..`，防止越界。读不受限。

```toml
[sandbox]
# workspace_root = ""          # 留空 = 当前目录
# allow_write    = ["/tmp"]    # 额外可写目录
```

`bash` 在 macOS 默认进沙盒（Seatbelt）：命令只能写这些 root（外加临时目录与工具链缓存），`[sandbox] network` 为真时才能联网。其它平台暂回退为不沙盒运行。

---

## 十二、记忆系统

### 12.1 层次文档

| 文件 | 作用域 | 说明 |
|------|--------|------|
| `Rexion.md` | 项目（提交共享） | 项目长期指令，类比 Claude Code 的 CLAUDE.md |
| `Rexion.local.md` | 个人（git-ignored） | 个人本地指令 |
| `~/.config/Rexion/Rexion.md` | 用户全局 | 跨项目 |
| 祖先目录的 `Rexion.md` | 向上继承 | monorepo 支持 |
| `AGENTS.md` | 回退名 | 当作 `Rexion.md` 的备选名 |

### 12.2 记忆操作

- `@path` 单独一行：导入另一个文件内容
- `#<note>` 在聊天中快速追加一行到 `Rexion.md`
- `remember` 工具：保存持久事实到 per-project 自动记忆库（frontmatter 文件 + `MEMORY.md` 索引），下个会话加载进前缀

### 12.3 缓存稳定性

系统提示前缀（base prompt + tools + memory）必须跨轮次**字节稳定**，以保持 DeepSeek 自动前缀缓存命中。永远不要在会话中修改前缀——变更走轮次尾部（见 `control.Compose`）。

---

## 十三、双模型协同

### 13.1 执行器 + 规划器

`Rexion setup` 保持首次体验极简；双模型协同需手动配置：

```toml
[agent]
planner_model = "deepseek-pro"   # 低频规划器
```

- 规划器看到已加载的记忆，拿到一小组只读研究工具，先检查相关文件再出计划
- 写入类和流程类工具只给执行器
- 各自独立、缓存稳定的 session

### 13.2 Subagent 模型

```toml
[agent]
subagent_model = "deepseek-pro"                          # 统一覆盖
subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }  # 指定 skill 覆盖
```

默认继承执行器模型。

### 13.3 计划模式

交互式前端中计划模式默认手动开启。设置 `agent.auto_plan = "on"` 后，看起来复杂的任务会自动进入 plan mode：先只读生成计划，待用户批准后才编辑文件或执行有副作用命令。

- `auto_plan_classifier` 指定便宜的 provider（如 `deepseek-flash`），只在边界输入上调用，分类失败回退启发式
- `/auto-plan off|on`：会话内修改用户级设置
- `Rexion config auto-plan off|on [--local]`：shell/脚本修改（`--local` 写项目级覆盖）

---

## 十四、故障排查

### 14.1 doctor 命令

```sh
Rexion doctor          # 文本诊断报告
Rexion doctor --json   # JSON 格式
```

### 14.2 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `md_to_pdf` 不可用 | pandoc 未安装或未加入 PATH | 安装 pandoc 并重启 |
| 模板库为空 | `.Rexion/templates/` 不存在或无文件 | 创建目录并放入模板 |
| 文档预览显示「无法预览」 | docx 页级渲染未实现 | 点击「用默认应用打开」 |
| `read_sheet` 返回空 | 路径错误或格式不支持 | 确认是 xlsx/csv 且路径正确 |
| `query_sheet` 报错 | SQL 语法错误或字段不存在 | 检查字段名和 WHERE 语法 |
| `web_search` 不可用 | 未设 `SEARCH_API_KEY` | 配置环境变量 |
| slides 带水印 | 未设 `UNIDOC_LICENSE_API_KEY` | 申请免费 key |
| 日历集成不可用 | 非 Windows/macOS 或缺 Outlook/Calendar.app | 使用对应平台 |
| 桌面端白屏 | `frontend/dist` 未构建 | `cd desktop/frontend && pnpm build` |
| Linux WebKitGTK 渲染异常 | 4.0/4.1 版本不匹配 | 加 `-tags webkit2_41` 构建 |
| macOS 首次启动「已损坏」 | Gatekeeper 隔离属性 | `xattr -dr com.apple.quarantine /Applications/Rexion.app` |
| Windows SmartScreen 拦截 | 未签名 | 「更多信息 → 仍要运行」 |

### 14.3 日志

MCP 插件日志输出到 stderr（stdout 保留给 JSON-RPC）。Rexion 自身日志按 verbosity 输出。

### 14.4 验证下载产物

产物用 minisign 签名（公钥 ID `AF12CA46F4A9EBB0`）：

```sh
minisign -Vm Rexion-darwin-arm64.zip \
  -P RWSw66n0RsoSr6Zhh6qt5YO95YkpCayTOCMFVDNUQSjJYwxoYngNVBSq
```

---

## 附录：技术架构

```
┌─────────────────────────────────────────────────────────┐
│              前端层（chat TUI / HTTP+SSE / Wails 桌面）   │
└───────────────────────────┬─────────────────────────────┘
                            │  transport-agnostic
┌───────────────────────────▼─────────────────────────────┐
│              control.Controller（内核，单一入口）         │
│   Provider registry · Tool registry · Gate · Compose    │
└───────────────────────────┬─────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐  ┌──────────────────┐  ┌──────────────┐
│  内置工具     │  │  MCP 插件（子进程）│  │  Skill 系统   │
│  (init 自注册)│  │  stdio / HTTP    │  │  inline/subagent│
└──────────────┘  └──────────────────┘  └──────────────┘
```

三层可扩展性，全部藏在内核按名解析的 registry 之后：

1. **Registry**：`Provider` 与 `Tool` 是接口；内核没有 `switch model`
2. **编译期内置**：provider 和 tool 通过 `init()` 自注册，`main` 用 blank import 拉入。新增内置 = 一个文件 + 一行 import
3. **运行时插件**：配置里声明的可执行文件，通过 stdin/stdout 上的 newline-delimited JSON-RPC 2.0（MCP 约定）通信

---

*文档版本：基于 main-v2 分支 · 更新日期：2026-07-02*
