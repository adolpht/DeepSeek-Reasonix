# Rexion configuration.
# Resolution order: flag > ./Rexion.toml > ~/.config/Rexion/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REXION_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps   = 0
temperature = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside Rexion

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "ToolName" or "ToolName(glob)"; '*' matches any run, '?' one char.
mode  = "ask"
# deny = ["bash(rm -rf*)", "bash(git push*)"]   # hard-blocked in every mode
allow = ["write_file=content/项目概述/ProjectOverview.md", "write_file=content/架构设计/Architecture.md", "write_file=content/架构设计/LayeredDesign/LayeredDesign.md", "write_file=content/架构设计/CacheFirstDesign.md", "write_file=content/配置系统/ConfigSystem.md", "write_file=content/业务模块-记忆系统/MemorySystem.md", "write_file=content/业务模块-技能系统/SkillSystem.md", "write_file=content/业务模块-权限与审批/PermissionSystem.md", "write_file=content/桌面前端/DesktopFrontend.md", "write_file=content/开发指南/DevGuide.md", "write_file=content/故障排除/Troubleshooting.md", "write_file=content/Quickstart.md", "write_file=content/README.md", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && echo \"=== Document Statistics ===\" && echo \"Total Markdown files: $(find content/ -name '*.md' | wc -l)\" && echo \"\" && echo \"=== Per-Document Structure Check ===\" && for f in $(find content/ -name '*.md'); do name=$(basename \"$f\"); chapters=$(grep -c '^## ' \"$f\"); has_cite=$(grep -c '<cite>' \"$f\"); mermaids=$(grep -c '```mermaid' \"$f\"); sources=$(grep -c 'Diagram Sources\\|Chapter Sources' \"$f\"); line_refs=$(grep -oP '\\[.*?\\]\\(.*?#L\\d+.*?\\)' \"$f\" 2>/dev/null | wc -l); echo \"[${chapters}ch/${has_cite}cite/${mermaids}mermaid/${sources}src/${line_refs}ref] $name\"; if [ \"$chapters\" -lt 8 ]; then echo \"  WARNING: Chapter count low (<8): $name\"; fi; if [ \"$has_cite\" -eq 0 ]; then echo \"  ERROR: Missing <cite>: $name\"; fi; if [ \"$mermaids\" -eq 0 ]; then echo \"  WARNING: No Mermaid diagrams: $name\"; fi; if [ \"$line_refs\" -lt 3 ]; then echo \"  WARNING: Few line references (<3): $name\"; fi; done", "bash=cd \"D:/办公文件/项目文件/AI相关/DeepSeek-Rexion\" && find content/ -name \"*.md\" | wc -l", "bash=find content/ -name \"*.md\" | wc -l", "bash=for f in $(find content/ -name \"*.md\"); do name=$(basename \"$f\"); chapters=$(grep -c '^## ' \"$f\" 2>/dev/null); has_cite=$(grep -c '<cite>' \"$f\" 2>/dev/null); mermaids=$(grep -c 'mermaid' \"$f\" 2>/dev/null); line_refs=$(grep -o '#L[0-9]' \"$f\" 2>/dev/null | wc -l); echo \"[${chapters}ch/${has_cite}cite/${mermaids}m/${line_refs}ref] $name\"; done", "bash=find content/ -name \"*.md\" -exec echo \"=== {} ===\" \\; -exec grep -c \"^## \" {} \\;", "bash=find content/ -name \"*.md\" ! -name \"README.md\" -exec echo \"=== {} ===\" \\; -exec grep -c '<cite>' {} \\; -exec grep -c 'mermaid' {} \\; -exec grep -o '#L[0-9]*' {} \\; -exec echo \"---\" \\;", "bash=echo \"=== Final Document Statistics ===\" && echo \"\" && echo \"Total Markdown files:\" && find content/ -name \"*.md\" | wc -l && echo \"\" && echo \"Total size:\" && du -sh content/ && echo \"\" && echo \"Per document:\" && for f in $(find content/ -name \"*.md\" | sort); do size=$(wc -c < \"$f\"); lines=$(wc -l < \"$f\"); echo \"  $f — ${lines} lines, ${size} bytes\"; done", "bash=find content/ -name \"*.md\" | while read f; do lines=$(wc -l < \"$f\"); echo \"$f — $lines lines\"; done", "doc_reviewer", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\项目概述\\ProjectOverview.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\架构设计\\Architecture.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\架构设计\\LayeredDesign\\LayeredDesign.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\架构设计\\CacheFirstDesign.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\配置系统\\ConfigSystem.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\业务模块-记忆系统\\MemorySystem.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\业务模块-技能系统\\SkillSystem.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\业务模块-权限与审批\\PermissionSystem.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\桌面前端\\DesktopFrontend.md", "write_file=D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\content\\开发指南\\DevGuide.md", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && echo \"=== Document Statistics ===\" && echo \"Total Markdown files: $(find content/ -name '*.md' | wc -l)\" && echo \"\" && echo \"=== Per-Document Structure Check ===\" && for f in $(find content/ -name '*.md' | sort); do name=$(basename \"$f\"); chapters=$(grep -c '^## ' \"$f\"); has_cite=$(grep -c '<cite>' \"$f\"); mermaids=$(grep -c '```mermaid' \"$f\"); sources=$(grep -c 'Diagram Sources\\|Chapter Sources' \"$f\"); line_refs=$(grep -oP '\\[.*?\\]\\(.*?#L\\d+.*?\\)' \"$f\" 2>/dev/null | wc -l); echo \"[${chapters}ch/${has_cite}cite/${mermaids}mermaid/${sources}src/${line_refs}ref] $name\"; if [ \"$chapters\" -lt 8 ]; then echo \"  WARNING: Chapter count low (<8): $name\"; fi; if [ \"$has_cite\" -eq 0 ]; then echo \"  ERROR: Missing <cite>: $name\"; fi; if [ \"$mermaids\" -eq 0 ]; then echo \"  WARNING: No Mermaid diagrams: $name\"; fi; if [ \"$line_refs\" -lt 3 ]; then echo \"  WARNING: Few line references (<3): $name\"; fi; done", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && grep -n '#L' content/项目概述/ProjectOverview.md | head -20", "explore", "multi_edit=desktop/frontend/src/App.tsx", "edit_file=desktop/frontend/src/styles.css", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && dir /b package.json 2>nul && type package.json | findstr /i \"build\"", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && type package.json | findstr /i \"build\\|check\\|tsc\\|vite\"", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && npx tsc --noEmit 2>&1 | head -30", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && node scripts/check-css-syntax.mjs src/styles.css 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && node scripts/check-z-index-tokens.mjs src/styles.css 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && npx tsc --noEmit 2>&1", "bash=npx tsc --noEmit", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && npx --no-install tsc --noEmit 2>&1", "edit_file=internal/control/controller.go", "edit_file=internal/permission/permission.go", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go build ./internal/permission/... 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go build ./internal/control/... 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go test ./internal/permission/... 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go test ./internal/control/... 2>&1", "multi_edit=internal/permission/permission_extra_test.go", "edit_file=internal/permission/permission_test.go", "edit_file=internal/permission/permission_extra_test.go", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go test ./internal/control/... ./internal/config/... ./internal/boot/... 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go build ./... 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go build ./...", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && npx --no-install tsc --noEmit", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go test ./internal/permission/...", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && go test ./internal/control/... ./internal/config/...", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && which wails 2>/dev/null || wails version 2>&1 || echo \"wails not found\"", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\" && go version 2>&1 && echo \"---\" && node --version 2>&1 && echo \"---\" && pnpm --version 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\" && wails version 2>&1", "bash=makensis -VERSION 2>&1 && echo \"NSIS found\" || echo \"NSIS not found\"", "bash=ls \"/c/Program Files (x86)/NSIS/\" 2>/dev/null && echo \"NSIS found at standard path\" || echo \"NSIS not at standard path\"", "bash=winget list NSIS 2>&1 || choco list nsis 2>/dev/null || echo \"no package manager found\"", "bash=PATH=\"/c/Program Files (x86)/NSIS:$PATH\" makensis -VERSION 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\\frontend\" && pnpm install 2>&1", "bash=cd \"D:\\办公文件\\项目文件\\AI相关\\DeepSeek-Rexion\\desktop\" && PATH=\"/c/Program Files (x86)/NSIS:$PATH\" wails build -clean -platform windows/amd64 -nsis -ldflags \"-X main.version=v1.0.0\" 2>&1", "bash", "edit_file", "multi_edit", "write_file", "apply_patch", "research", "task", "kill_shell", "delete_range", "remember", "forget", "mcp__office__write_docx", "mcp__im__create_im_session", "mcp__im__mark_command_done"]
# ask = ["write_file"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# NOTE: Common MCP plugins (office, sheet, slides, search, mail, browser,
# design, computer) are configured globally in ~/.config/Rexion/config.toml so they
# are available across all projects.
