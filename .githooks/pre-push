#!/bin/sh
# Go-native pre-push gate replacing the retired v1 npm hook (install: make hooks).
# Full tests run in CI; vet + binary guard are the fast, cross-platform local checks.
set -e
go vet ./...
./scripts/check-tracked-binaries.sh
